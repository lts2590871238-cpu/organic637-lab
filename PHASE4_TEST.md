# Phase 4 最小人工验收

部署后只测这些高价值路径，不需要全站反复巡检。

1. 登录后顶部出现“侦探”。
2. 点击“侦探”，能看到 5 个案例。
3. 打开“谁是真正的端炔？”：
   - DBE 输入后能继续；
   - IR 谱图可见；
   - NMR 谱图可见；
   - 候选可分别选择“保留/排除/不确定”；
   - 三个候选都处理后才能进入最终结构；
   - 提交后出现分项得分和证据矩阵。
4. 故意把最终候选选错，但前面 DBE/IR/NMR 做对：结果不能显示成 0 分。
5. 返回侦探所，刚完成的案例应显示“已完成 · xx%”。
6. 打开顶部“能力”，结构推断相关 skill 应出现学习证据。
7. 刷新网页，侦探案例完成状态仍在。
8. Day 1–4 原有学习、排序、路线、电子箭头仍能继续使用。

本轮不需要修改 Cloudflare Worker 或 D1。
